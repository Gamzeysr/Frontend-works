console.log("***** 2 - RECURSION *******")

//! RECURSION FONK kendi kendine çağıran fonksiyondur

// const fibo = (n) => {
    // if (n <= 0) {
        // return 0;
    // } else if (n === 1 || n === 2) {
        // return 1;
//
    // } else {
        // return fibo(n - 1) + fibo(n - 2);
    // }
// };
//
// const n = +prompt("n terişmini giriniz:");
// console.log(`FIBO(${n})= ${fibo(n)}`);